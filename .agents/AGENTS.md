# AGENTS.md - Workspace Rules

- Always synchronize local changes with the GitHub repository.
- Always commit as Ahmed Xuhri using the human identity credentials:
  `git -c user.email="248666654+ahmedxuhri@users.noreply.github.com" -c user.name="Ahmed Xuhri"`
- Always use the `gh` CLI to perform GitHub operations (repository creation, authentication checks, pushes).
- Do NOT attempt to run or push through CI/CD workflows, because CI/CD is currently disabled by GitHub due to a billing issue.
